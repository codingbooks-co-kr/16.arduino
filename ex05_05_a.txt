// LED 밝기 조절(for문)
// [질문5.2]

const int LED = 6;

void setup() {
  pinMode(LED, OUTPUT);
}

void loop() {
  for (int i = 0; i < 256; i++) {
    analogWrite(LED, i);
    delay(10);
  }
  digitalWrite(LED, 0);
  delay(500);
}