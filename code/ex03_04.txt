// 푸시버튼값 읽고 출력하기

const int BUTTON = 2;			// 전역변수 (푸시버튼 핀 번호)

void setup() {
  pinMode(BUTTON, INPUT);
  Serial.begin(9600);
}

void loop() {
  int value = digitalRead(BUTTON);		// 지역변수 (푸시버튼 값 읽기)
  Serial.print("Button: ");
  Serial.println(value);
  delay(100);
}