// 가변저항값 읽고 출력

const int VARIABLE_R = A1;

void setup() {
  pinMode(VARIABLE_R, INPUT);
  Serial.begin(9600);
}

void loop() {
  int value = analogRead(VARIABLE_R);	// 가변저항 값 읽기
  Serial.print("Variable Resistor: ");
  Serial.println(value);			// 가변저항 값을 시리얼 모니터로 전송 후 출력
  delay(100);
}