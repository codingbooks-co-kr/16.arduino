// 조도센서값 읽고 출력

const int LIGHT = A3;

void setup() {
  pinMode(LIGHT, INPUT);
  Serial.begin(9600);
}

void loop() {
  int value = analogRead(LIGHT);	// 조도센서 값 읽기
  Serial.print("Light: ");
  Serial.println(value);		// 조도센서 값을 시리얼 모니터로 전송 후 출력
  delay(100);
}